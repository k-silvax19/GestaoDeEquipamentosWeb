@{
    Layout = "_Layout";
    ViewBag.Titulo = "Edição de Equipamento";

    List<ListarFabricanteViewModel> fabricantes = ViewBag.Fabricantes;
}

@model EditarEquipamentosViewModel



<form class="formulario" action="/Equipamento/Editar/@Model.Id" method="post">

    <p class="formulario-alerta">Deseja realmente excluir esse equipamento?</p>

    <div class="formulario-campo">
        <label>Nome</label>
        <input type="text" name="Nome" value="@Model.Nome">
    </div>

    <div class="formulario-campo">
        <label>Preço de Aquisição</label>
        <input type="number" name="PrecoAquisicao" min="0.01" step="0.01" value="@Model.PrecoAquisicao">
    </div>
    
    <div class="formulario-campo">
        <label>Data de Fabricação</label>
        <input type="date" name="DataFabricacao" value="@Model.DataFabricacao">
    </div>

    <div class="formulario-campo">
        <label>Fabricante</label>
        <select name="FabricanteId">
            @foreach (ListarFabricanteViewModel f in fabricantes)
            {
                if (f.Id == Model.FabricanteId)
                {
                    <option value="@f.Id" selected>@f.Nome</option>
                }
                else
                {
                    <option value="@f.Id">@f.Nome</option>
                }
            }
        </select>
    </div>

    <div class="formulario-acoes">
        <a href="/Equipamento/Listar">Voltar</a>
        <button type="submit">Confirmar</button>
    </div>
</form>